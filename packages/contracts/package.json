{
  "name": "@0gclawforge/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network 0g-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network 0g-mainnet",
    "verify:contracts": "hardhat verify --network 0g-testnet",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0"
  }
}
